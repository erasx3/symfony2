{% extends '::base.html.twig' %}

{% block body -%}
    {{ parent()}}
    <h1>Editar Proveedor</h1>

    {{ form(edit_form) }}
    {{ form(delete_form) }}


    <div class="well bs-component">
        <a class="btn btn-primary" href="{{ path('proveedor') }}">
            Volver
        </a>
    </div>


{% endblock %}
