{% extends '::base.html.twig' %}

{% block body -%}
{{ parent()}}

    <h2>Ingrese una Provincia</h2>

    {{ form(form) }}
 
        <ul class="record_actions">
    <li>
        <a href="{{ path('provincia') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
