{% extends '::base.html.twig' %}

{% block body -%}
{{ parent()}}
<h1 class="demo-section-title">Lista de Provincias</h1>
<table class="table table-list table-hover records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>               
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('localidad_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descripcion }}</td>               
                <td>
                <ul>
                    <li>
                        <a href="{{ path('localidad_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('localidad_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>



            
                    <a class="btn btn-primary" href="{{ path('provincia_new') }}">
                        Nueva Provincia
                    </a>
              
       
    {% endblock %}
