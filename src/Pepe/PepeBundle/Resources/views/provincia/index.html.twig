{% extends '::base.html.twig' %}

{% block body -%}
{{ parent()}}
<div class="container">
    <div class="row demo-row">
        <div class="col-xs-12">
            <h1 class="demo-section-title">Lista de Provincias</h1>
        </div>
    </div>
    <div class="row demo-row">
        <div class="col-xs-2">
            <h3 class="demo-panel-title">Id</h3>
        </div>
        <div class="col-xs-6">
            <h3 class="demo-panel-title">Descripcion</h3>
        </div>
        <div class="col-xs-4">
            <h3 class="demo-panel-title">Acciones</h3>
        </div>
    </div>

    {% for entity in entities %}
    <div class="row demo-row">
        <div class="col-xs-2">
            <a href="{{ path('provincia_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
        </div>
        <div class="col-xs-6">
            {{ entity.descripcion }}
            </div>
            <div class="col-xs-4">
                <ul>
                    <li>
                        <a href="{{ path('provincia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('provincia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </div>
        </div>
    {% endfor %}        


            <ul>
                <li>
                    <a href="{{ path('provincia_new') }}">
                        Create a new entry
                    </a>
                </li>
            </ul>
        </div>
    {% endblock %}
