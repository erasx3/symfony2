{% extends '::base.html.twig' %}

{% block body -%}
    {{ parent()}}
    <h1>detalle creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('detalle') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
