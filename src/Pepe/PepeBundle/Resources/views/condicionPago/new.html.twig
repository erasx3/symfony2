{% extends '::base.html.twig' %}

{% block body -%}
    {{ parent()}}
    <h1>Ingrese una nueva condicon de pago</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('condicionpago') }}">
            Volver a la Lista
        </a>
    </li>
</ul>
{% endblock %}
