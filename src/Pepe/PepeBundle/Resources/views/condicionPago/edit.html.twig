{% extends '::base.html.twig' %}

{% block body -%}
    {{ parent()}}
    <h1>Condiciones de Pago</h1>

    {{ form(edit_form) }}
    {{ form(delete_form) }}

    <div class="well bs-component">
        <a class="btn btn-primary"  href="{{ path('condicionpago') }}">
            Volver
        </a>
    </div>
{% endblock %}
