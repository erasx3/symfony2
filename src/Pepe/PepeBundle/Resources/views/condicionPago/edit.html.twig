{% extends '::base.html.twig' %}

{% block body -%}
    {{ parent()}}
    <h1>condicionPago edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('condicionpago') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
