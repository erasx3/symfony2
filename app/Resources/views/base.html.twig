<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link href="{{ asset('bundles/pepe/css/bootstrap.css')}}" type="text/css" rel="stylesheet"/>
            <link href="{{ asset('bundles/pepe/css/bootswatch.min.css')}}" type="text/css" rel="stylesheet"/>
            <link rel="stylesheet" href="http://code.jquery.com/ui/1.10.1/themes/base/jquery-ui.css" />
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}

            <div class="navbar navbar-default navbar-fixed-top">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-responsive-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">SysSold</a>
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Productos<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('producto')}}">Listar</a></li>
                                <li><a href="{{path('producto_new')}}">Agregar</a></li>
                            </ul>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Proveedores<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('proveedor')}}">Listar</a></li>
                                <li><a href="{{path('proveedor_new')}}">Agregar</a></li>
                            </ul>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Iva<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('iva')}}">Listar</a></li>
                                <li><a href="{{path('iva_new')}}">Agregar</a></li>
                            </ul>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Condiciones<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('condicionpago')}}">Listar</a></li>
                                <li><a href="{{path('condicionpago_new')}}">Agregar</a></li>
                            </ul>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Venta<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('factura')}}">Listar</a></li>
                                <li><a href="{{path('factura_new')}}">Agregar</a></li>
                            </ul>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Localidades<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('localidad')}}">Listar</a></li>
                                <li><a href="{{path('localidad_new')}}">Agregar</a></li>
                            </ul>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Provincia<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('provincia')}}">Listar</a></li>
                                <li><a href="{{path('provincia_new')}}">Agregar</a></li>
                            </ul>
                        </li>
                        </li>
                    </ul>
                    <ul class="nav navbar-right">
                        <li>
                            <a href="{{path('fos_user_security_logout')}}">logout</a>
                        </li>
                    </ul>
                </div>
            </div>
            {% if form is defined %}
                {% form_theme form 'PepeBundle:Default:form_div_layout.html.twig' %}
            {%endif%}            
            <div class="container">
            {% endblock %}
            {% block body2 %}
            </div>
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('bundles/pepe/js/jquery-1.8.3.min.js')}}" type="text/javascript"></script>

            <script src="{{ asset('bundles/pepe/js/bootstrap.min.js')}}" type="text/javascript"></script>
            <script src="{{ asset('bundles/pepe/js/jquery-ui.min.js')}}" type="text/javascript"></script>

        {% endblock %}
    </body>
</html>
